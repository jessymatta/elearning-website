import "./title.css"

const Title = ({title}) => {
    return (
        <div className='title'>
            <h1 className="title-name">{title}</h1>
        </div>
    )
}
export default Title

// Instructors Lis